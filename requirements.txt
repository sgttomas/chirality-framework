# Core dependencies  
neo4j>=5.0.0
python-dotenv>=1.0.0
click>=8.0.0

# Optional dependencies
# Use a version that supports the Responses API (client.responses.create)
openai>=1.42.0  # Only needed for OpenAI-based CellResolver

# Development dependencies (optional)
pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=22.0.0
# mypy>=1.0.0
